# Changelog

## 0.1.0

- Initial release
- Explorer tree view listing local TCP listeners from `node` processes, grouped by command
- Inline actions: kill process (SIGTERM with SIGKILL fallback), open in Simple Browser, open in system browser
- Title-bar refresh action
- Auto-refresh on view visibility
